# 架构边界

当前代码只有两个职责明确的 crate：`arreat-core` 保存跨入口共享的项目名称，
`arreat-app` 提供最小命令行入口。这是工程基线，不是产品架构的预先实现。

未来可能探索配置、桌面界面、静态模组、只读进程数据、物品、存储和市场等领域。
这些名称只是问题空间，不表示对应接口、crate 或依赖已经存在。每个领域只有在真实
行为和边界得到验证后才会进入代码库；特别是市场方向必须先完成 DD373 真实数据的
可行性验证。

Linux 是一等目标，Windows 必须保持可编译。用户可见文字使用简体中文；代码标识符
和协议使用英文。
